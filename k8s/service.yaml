apiVersion: v1
kind: Service
metadata:
  name: research-agent
spec:
  type: LoadBalancer  # Or NodePort for local minikube
  selector:
    app: research-agent
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
